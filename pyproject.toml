[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "omnivoice-server"
version = "0.2.4"
description = "OpenAI-compatible HTTP server for OmniVoice TTS"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "zamery", email = "matthew.ngo1114@gmail.com" }]

keywords = ["tts", "text-to-speech", "omnivoice", "openai", "fastapi", "voice-cloning"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
]

dependencies = [
    # Pin omnivoice to a compatible minor series.
    # 0.2.x adds pad_duration/fade_duration (0.2.0) and normalize_text +
    # VoiceClonePrompt.save()/load() + asr_device (0.2.1). generate() keeps
    # **kwargs passthrough, so OmniVoiceAdapter.build_kwargs() stays valid.
    # Widen this bound only after reviewing upstream changelogs.
    "omnivoice>=0.2.1,<0.3.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-multipart>=0.0.12",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "platformdirs>=3.0.0",
    "psutil>=6.0.0",
    "huggingface-hub>=0.26.0",
    "soundfile>=0.12.0",
]

[project.optional-dependencies]
formats = ["pydub>=0.25.0"]  # For mp3, opus, aac, flac support (also requires ffmpeg)
benchmark = ["psutil", "tqdm"]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "httpx>=0.27.0",
    "ruff>=0.6.0",
    "mypy>=1.11.0",
    # Keep torchcodec off Windows in the default dev extra.
    # Windows users can still opt in manually if they align torch/torchaudio/torchcodec.
    "torchcodec>=0.11; sys_platform != 'win32'",
]

[project.urls]
Homepage = "https://github.com/NguyenNinh05/omnivoice-server"
Documentation = "https://github.com/NguyenNinh05/omnivoice-server#readme"
Repository = "https://github.com/NguyenNinh05/omnivoice-server"
Issues = "https://github.com/NguyenNinh05/omnivoice-server/issues"
Changelog = "https://github.com/NguyenNinh05/omnivoice-server/blob/main/CHANGELOG.md"
"Upstream project" = "https://github.com/maemreyo/omnivoice-server"

[project.scripts]
omnivoice-server = "omnivoice_server.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["omnivoice_server"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.10"
strict = false
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["omnivoice_server"]
branch = true
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__main__.py",
    "*/conftest.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
precision = 1
show_missing = true
skip_covered = false

[tool.coverage.html]
directory = "htmlcov"
